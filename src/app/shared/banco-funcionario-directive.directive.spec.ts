import { BancoFuncionarioDirectiveDirective } from './banco-funcionario-directive.directive';

describe('BancoFuncionarioDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BancoFuncionarioDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
